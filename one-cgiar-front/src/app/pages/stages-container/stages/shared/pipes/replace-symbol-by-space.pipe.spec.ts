import { ReplaceSymbolBySpacePipe } from './replace-symbol-by-space.pipe';

describe('ReplaceSymbolBySpacePipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceSymbolBySpacePipe();
    expect(pipe).toBeTruthy();
  });
});
