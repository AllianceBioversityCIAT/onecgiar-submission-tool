import { ShowActivePipe } from './show-active.pipe';

describe('ShowActivePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowActivePipe();
    expect(pipe).toBeTruthy();
  });
});
