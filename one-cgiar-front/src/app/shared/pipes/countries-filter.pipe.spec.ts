import { CountriesFilterPipe } from './countries-filter.pipe';

describe('CountriesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CountriesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
