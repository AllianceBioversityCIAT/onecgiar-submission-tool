import { CoordinatorFilterPipe } from './coordinator-filter.pipe';

describe('CoordinatorFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CoordinatorFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
