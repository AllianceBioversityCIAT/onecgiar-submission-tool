import { RegionsFilterPipe } from './regions-filter.pipe';

describe('RegionsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RegionsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
